#!/bin/sh

# Install NODEJS NPM latest version by upgrade in place.

# This script will install NPM to latest versions.

# After upgrade, NPM will be latest and compatible with
# same version used on BUILD process

sudo npm install -g npm@latest