export const logger = (...params) => {
  // if (process.env.NODE_ENV === 'development') {
  //console.log(...params)
  // }
}

// class LoggerService {
//   static log(...params) {
//     if (process.env.NODE_ENV === "development") {
//       console.log(...params);
//     }
//   }
// }

// export default LoggerService;
